// This page is intentionally left without a default export to resolve
// a Next.js routing conflict. The active route is /events/[slug].
export async function generateStaticParams() {
  return [];
}
